<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.waimai.auth.mapper.AdminUserMapper">

    <select id="selectByUsernameNotDeleted"
            parameterType="string"
            resultType="com.school.waimai.auth.entity.AdminUser">
        SELECT
            id,
            username,
            password_hash,
            salt,
            status,
            last_login_time,
            create_time,
            update_time,
            deleted
        FROM admin_user
        WHERE username = #{username}
          AND deleted = 0
        LIMIT 1
    </select>

    <update id="updateLastLoginTime">
        UPDATE admin_user
        SET last_login_time = #{lastLoginTime},
            update_time = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>
</mapper>
