spring:
  datasource:
    url: jdbc:mysql://localhost:3306/waimai_dev?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672

logging:
  level:
    com.school.waimai: debug
